CREATE DATABASE ALIAS_PRACTICE;
USE ALIAS_PRACTICE;

-- In This Chapter We'll discuss about alias in SQL

-- ALIAS -- An alias is created with the *AS* keyword and is often used to make column name readable,

-- Let's see the Syntax:
SELECT Column_Nams AS Alias_name
FROM table_name;

-- Let's See the Example

SELECT StudentName AS NAME 
FROM STUDENT;

-- AND THATS IT 